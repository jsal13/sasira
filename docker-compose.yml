services:
  sasira-webapp:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./sources:/app/sources:ro
      - ./reference:/app/reference:ro
    environment:
      - FLASK_ENV=development
    restart: unless-stopped